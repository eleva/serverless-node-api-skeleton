type: array
items:
  $ref: '#/definitions/User'

definitions:
  User:
    type: object
    description: User object.
    required:
      - id
    properties:
      id:
        type: integer
        description: User unique identifier.
      email:
        type: string
        description: User email.
